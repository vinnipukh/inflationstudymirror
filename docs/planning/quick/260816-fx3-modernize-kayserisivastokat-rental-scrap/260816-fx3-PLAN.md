---
mode: quick
quick_id: 260816-fx3
description: Modernize KayseriSivasTokat rental scraper — ilanId, 3+1 filter, live sitekey, quoted selectors
must_haves:
  truths:
    - ilanId comes from tr[data-id] in the search table (recon-verified)
    - Current CSV schema is District,Rooms,Price (all dates, 3 cities)
    - Turnstile sitekey must be read live from #cloudflareTurnStileSiteKey (scraper.py:904 key is stale)
    - Compliance-locked fields: ilanId, District, Rooms, Price ONLY (law-faculty confirmed, APPROACH.md B0)
    - Browser-only modernization — no new driver, no wreq, no detail pages
  artifacts:
    - Modified config.py (ROOMS_FILTER), scraper.py (parse_page/save_incremental/_is_enterprise_turnstile), main.py (--rooms)
    - .planning/quick/260816-fx3-*/SUMMARY.md
  key_links:
    - docs/APPROACH.md (B0 compliance + B6 selector rules)
    - docs/sahibinden-recon-2026-08-15.md (table structure; merged into APPROACH.md §7)
---

# Plan: Modernize rental scraper (3+1 kiralık)

Surgical modernization of `InflationItems/Codes/HousesRent/KayseriSivasTokat/` — keep the working
rayobrowse/Playwright architecture, fix the known pain points. NO new driver,
NO wreq, NO detail-page crawling.

## Task 1 — parse_page: capture ilanId + robust rows

**Files:** `InflationItems/Codes/HousesRent/KayseriSivasTokat/scraper.py`
**Action:**
- Row selector: `#searchResultsTable tbody tr[data-id]` primary (quoted attribute selector per B6), fallback to `tr.searchResultsItem`
- Extract `ilanId = row.get("data-id")` on every row
- Keep District/Rooms/Price extraction unchanged
- Apply `config.ROOMS_FILTER`: when set (e.g. "3+1"), keep only rows whose Rooms matches exactly (after strip); unmatched rows dropped
**Verify:** inline python fixture — parse_page on a minimal HTML table with a `data-id` row returns `ilanId`, and `ROOMS_FILTER="3+1"` keeps only the 3+1 row.
**Done:** parse_page returns `{"ilanId", "District", "Rooms", "Price"}` per record.

## Task 2 — CSV schema + rooms filter config + CLI

**Files:** `InflationItems/Codes/HousesRent/KayseriSivasTokat/config.py`, `scraper.py` (save_incremental), `InflationItems/Codes/HousesRent/KayseriSivasTokat/main.py`
**Action:**
- config: add `ROOMS_FILTER = "3+1"` (default per user request; `None` = all) + comment noting compliance scope
- `save_incremental`: fieldnames `["ilanId", "District", "Rooms", "Price"]`
- main.py: add `--rooms` CLI flag (e.g. `--rooms 3+1`); overrides `config.ROOMS_FILTER`; `None`/empty = all
**Verify:** py_compile all three files; save_incremental header written with 4 columns.
**Done:** CSV header `ilanId,District,Rooms,Price`.

## Task 3 — Live Turnstile sitekey

**Files:** `InflationItems/Codes/HousesRent/KayseriSivasTokat/scraper.py`
**Action:**
- `_is_enterprise_turnstile`: add method — read `#cloudflareTurnStileSiteKey` value (recon-verified live sitekey input); prefer it over the stale hardcoded `#sitekeyEnterprise` value
- Where the enterprise key is used (solve flow), prefer the page-read key, fallback to hardcoded constant
- Note stale-key comment updated
**Verify:** py_compile; grep confirms `cloudflareTurnStileSiteKey` used.
**Done:** sitekey read live from the page when present.

## Task 4 — Quoted selectors sweep (B6)

**Files:** `InflationItems/Codes/HousesRent/KayseriSivasTokat/scraper.py`
**Action:** audit `page.evaluate`/BS4 selectors for unquoted attribute values containing digits/dots (e.g. `[src*=...]`, `[data-id=...]`) → quote them. `script[src*="turnstile"]` already quoted; fix any others.
**Verify:** grep for unquoted `[attr=` patterns in the file.
**Done:** all attribute selectors quoted.
