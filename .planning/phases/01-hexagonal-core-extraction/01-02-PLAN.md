---
phase: "01"
plan: "01-02"
type: implementation
wave: 2
depends_on: ["01-01"]
autonomous: true
requirements: [REQ-02, REQ-05, REQ-06, REQ-07, REQ-08, REQ-10, REQ-11]
files_modified:
  - streamlit_app.py
  - inflation_dashboard/__init__.py
  - inflation_dashboard/application/__init__.py
  - inflation_dashboard/application/use_cases.py
  - inflation_dashboard/application/chart_specs.py
---

# Plan 01-02: Extract aggregation/use-case functions and add focused ad-hoc verification

<objective>
Extract the dashboard's product-history, retailer-average, price-mover, and coverage calculations into framework-independent application/use-case modules, add Plotly-ready chart specification dictionaries without importing Plotly in core code, and update `streamlit_app.py` to render from those core outputs. This plan depends on the loader boundary created by 01-01 and covers decisions D-03, D-04, D-05, D-06, D-07, D-08, D-11, D-12, D-17, D-18, D-19, and D-20 from `01-CONTEXT.md`.
</objective>

## Artifacts this phase produces

- `inflation_dashboard/application/__init__.py` — package marker for application/use-case functions.
- `inflation_dashboard/application/use_cases.py` — new use-case functions: `list_inventory_filters`, `get_product_history`, `summarize_product_history`, `calculate_retailer_average_trends`, `calculate_price_movers`, `calculate_coverage_summary`, `calculate_coverage_over_time`, and `calculate_category_coverage`.
- `inflation_dashboard/application/chart_specs.py` — chart-spec helpers: `product_price_chart_spec`, `retailer_average_chart_spec`, `coverage_area_chart_spec`, `category_coverage_bar_chart_spec`, and optional table-column constants for dashboard views.
- `streamlit_app.py` uses application functions and chart specs for product explorer, retailer averages, price movers, and coverage overview while retaining Streamlit and Plotly rendering at the UI boundary.
- OS temp verification script created during execution only, not committed.

<must_haves>
truths:
  - id: D-03
    statement: Core code may produce chart-spec dictionaries for dashboard charts, but Streamlit remains responsible for rendering.
    verification: `chart_specs.py` returns dictionaries and does not import Streamlit or Plotly.
  - id: D-04
    statement: `streamlit_app.py` imports and uses the new core use-case functions while preserving the four existing dashboard views.
    verification: Source assertions show `streamlit_app.py` imports from `inflation_dashboard.application` and still defines all four tab renderers.
  - id: D-05
    statement: Core chart functions return Plotly-ready dictionary specs, not Plotly `Figure` objects.
    verification: `chart_specs.py` contains dictionaries with keys such as `type`, `x`, `y`, `color`, `title`, `x_label`, `y_label`, and `table_columns`.
  - id: D-06
    statement: Chart specs are bounded to product explorer, retailer averages, price movers tables, and coverage overview charts.
    verification: Chart-spec functions are named for those existing dashboard views and no unrelated chart family is added.
  - id: D-07
    statement: Chart specs include chart type, x/y/color columns where applicable, titles, axis labels, and table column lists.
    verification: Temporary verification script asserts representative spec dictionaries contain these keys.
  - id: D-08
    statement: Core application modules do not import Plotly; Plotly conversion remains in `streamlit_app.py`.
    verification: Import scan finds no `plotly` text in `inflation_dashboard/application/`.
  - id: D-11
    statement: Public application/use-case calls are separate functions for inventory/filter metadata, product history, retailer averages, price movers, and coverage.
    verification: `use_cases.py` contains concrete `def` statements for those separate use cases.
  - id: D-12
    statement: Phase 1 preserves pandas dtypes internally and does not perform JSON serialization.
    verification: Application functions return pandas DataFrames/dicts containing pandas-derived values; no `.to_json` or Falcon response conversion is introduced.
  - id: D-17
    statement: Phase 1 verification uses an ad-hoc Python script under the OS temp directory against bounded real CSV data.
    verification: Executor runs a temp script path outside the repo and records command output in SUMMARY.md.
  - id: D-18
    statement: Verification asserts core modules do not import Streamlit or Falcon.
    verification: Temp script scans `inflation_dashboard/` and fails on forbidden framework imports.
  - id: D-19
    statement: Verification asserts expected columns and non-empty outputs for a small selected retailer/date/file-cap load when real data is available.
    verification: Temp script loads `max_files_per_retailer=1` for a selected retailer and checks columns and non-empty use-case outputs if inventory exists.
  - id: D-20
    statement: Importing and exercising changed code is sufficient; no separate `python -m py_compile` requirement is added.
    verification: Temp script imports all changed modules and exercises representative functions.
  - id: REQ-02
    statement: Use cases accept or preserve selected retailers, date range, and max files per retailer inputs passed through from the loader/API boundary.
    verification: Source and temp script show the loader call path and use cases operate over filtered `history_df`.
  - id: REQ-05
    statement: Product explorer use case exposes price history plus latest price, cheapest price/date, and change versus first observation.
    verification: `summarize_product_history` or equivalent returns keys for latest price, cheapest price, cheapest date, and change versus first.
  - id: REQ-06
    statement: Retailer averages use case supports average and median price trends by date and retailer.
    verification: `calculate_retailer_average_trends` accepts an aggregation parameter and uses mean/median grouping.
  - id: REQ-07
    statement: Price movers use case produces strongest drops and increases from loaded filtered history.
    verification: `calculate_price_movers` returns both `biggest_drops` and `biggest_gains` DataFrames or equivalent mapping entries.
  - id: REQ-08
    statement: Coverage use cases expose row counts, retailer coverage, date coverage, category coverage, and skipped-file diagnostics for UI display.
    verification: `calculate_coverage_summary`, `calculate_coverage_over_time`, and `calculate_category_coverage` operate on loaded history; `streamlit_app.py` still passes skipped diagnostics to the coverage renderer.
  - id: REQ-10
    statement: Application use cases import without Streamlit or Falcon.
    verification: Temp script imports `inflation_dashboard.application.use_cases` without importing Streamlit/Falcon.
  - id: REQ-11
    statement: Use-case extraction does not remove loader caps or force full eager CSV scans.
    verification: `main()` still calculates `effective_limit` and passes it to the extracted loader from 01-01.
</must_haves>

<tasks>
<task id="01-02-T1" type="refactor">
<name>Create application use-case functions for dashboard data calculations</name>
<read_first>
- `streamlit_app.py` functions `render_product_explorer`, `render_retailer_average`, `render_price_movers`, and `render_overview`.
- `inflation_dashboard/adapters/csv_price_repository.py` from plan 01-01.
- `.planning/phases/01-hexagonal-core-extraction/01-CONTEXT.md` decisions D-02, D-11, D-12, D-17, D-19, and D-20.
- `.planning/REQUIREMENTS.md` requirements REQ-05, REQ-06, REQ-07, REQ-08, REQ-10, and REQ-11.
</read_first>
<action>
Create `inflation_dashboard/application/use_cases.py` with DataFrame-first functions that encapsulate the existing dashboard calculations: `list_inventory_filters(inventory: pd.DataFrame) -> dict[str, object]`, `get_product_history(history: pd.DataFrame, retailer: str, product_name: str) -> pd.DataFrame`, `summarize_product_history(product_history: pd.DataFrame) -> dict[str, object]`, `calculate_retailer_average_trends(history: pd.DataFrame, selected_retailers: list[str], aggregation: str) -> pd.DataFrame`, `calculate_price_movers(history: pd.DataFrame, selected_retailer: str, mover_count: int) -> dict[str, pd.DataFrame]`, `calculate_coverage_summary(history: pd.DataFrame, skipped: pd.DataFrame) -> dict[str, object]`, `calculate_coverage_over_time(history: pd.DataFrame) -> pd.DataFrame`, and `calculate_category_coverage(history: pd.DataFrame, limit: int = 20) -> pd.DataFrame`. Preserve the existing formulas from `streamlit_app.py`, including latest/cheapest/change metrics, mean/median aggregation, price mover formulas, and coverage groupings. Do not import Streamlit, Falcon, or Plotly. Do not serialize to JSON in this phase.
</action>
<acceptance_criteria>
- `inflation_dashboard/application/use_cases.py` exists.
- `use_cases.py` contains all eight named functions in the action.
- `calculate_retailer_average_trends` supports both `Average` and `Median` aggregation strings and uses `mean()`/`median()` grouping by `date` and `retailer`.
- `calculate_price_movers` computes `change_since_first_pct`, `drop_from_peak_pct`, and `savings_vs_peak`, and returns strongest drops plus strongest gains.
- `calculate_coverage_summary` includes retailer count, product count, observation count, date range, and skipped-file count or skipped diagnostics summary.
- `use_cases.py` contains no `streamlit`, `falcon`, `plotly`, `.to_json`, or JSON response conversion code.
</acceptance_criteria>
<verify>
Run `python - <<'PY'` with a small in-memory pandas DataFrame containing at least two retailers, two dates, and repeated products; import and exercise each use-case function; assert average/median outputs, mover keys, and coverage keys are present.
</verify>
</task>

<task id="01-02-T2" type="refactor">
<name>Create Plotly-ready chart specification dictionaries without Plotly imports</name>
<read_first>
- `streamlit_app.py` chart calls in `line_chart`, `render_product_explorer`, `render_retailer_average`, and `render_overview`.
- `inflation_dashboard/application/use_cases.py` from task 01-02-T1.
- `.planning/phases/01-hexagonal-core-extraction/01-CONTEXT.md` decisions D-03, D-05, D-06, D-07, and D-08.
</read_first>
<action>
Create `inflation_dashboard/application/chart_specs.py` with functions that return dictionaries, not Plotly figures: `product_price_chart_spec(title: str)`, `retailer_average_chart_spec(aggregation: str)`, `coverage_area_chart_spec()`, and `category_coverage_bar_chart_spec()`. Each spec must include chart metadata required by Streamlit to create Plotly figures: `type`, `x`, `y`, `color` where applicable, `title`, `x_label`, `y_label`, and `table_columns`. Include table-column lists for product history, retailer averages, biggest drops, biggest gains, skipped diagnostics, coverage over time, and category coverage. Do not import Streamlit, Falcon, or Plotly.
</action>
<acceptance_criteria>
- `inflation_dashboard/application/chart_specs.py` exists.
- Each chart-spec function returns a `dict` with `type`, `x`, `y`, `title`, `x_label`, `y_label`, and `table_columns` keys; specs needing color include a `color` key.
- The module contains table column lists covering `date`, `price`, `category`, `source_file`, `retailer`, `latest_price`, `max_price`, `savings_vs_peak`, `drop_from_peak_pct`, `first_price`, `change_since_first_pct`, `tracked_products`, and `products` where those tables are rendered.
- The module contains no `streamlit`, `falcon`, or `plotly` imports/text.
</acceptance_criteria>
<verify>
Run `python - <<'PY'` to import every chart-spec function, call each one, and assert required dictionary keys are present and no returned object has a Plotly `to_plotly_json` attribute.
</verify>
</task>

<task id="01-02-T3" type="refactor">
<name>Wire Streamlit renderers to use extracted use cases and chart specs</name>
<read_first>
- `streamlit_app.py` full file after plan 01-01.
- `inflation_dashboard/application/use_cases.py` from task 01-02-T1.
- `inflation_dashboard/application/chart_specs.py` from task 01-02-T2.
- `.planning/phases/01-hexagonal-core-extraction/01-CONTEXT.md` decisions D-03, D-04, D-05, D-06, D-07, D-08, and D-12.
</read_first>
<action>
Update `streamlit_app.py` so `render_product_explorer` uses `get_product_history` and `summarize_product_history`; `render_retailer_average` uses `calculate_retailer_average_trends`; `render_price_movers` uses `calculate_price_movers`; and `render_overview` uses `calculate_coverage_summary`, `calculate_coverage_over_time`, and `calculate_category_coverage`. Keep the Streamlit controls and tab names unchanged. Keep Plotly imports and figure creation in `streamlit_app.py`; build figures from chart-spec dictionaries by reading spec keys such as `type`, `x`, `y`, `color`, and `title`. Do not make Streamlit call Falcon or HTTP in this phase.
</action>
<acceptance_criteria>
- `streamlit_app.py` imports from `inflation_dashboard.application.use_cases` and `inflation_dashboard.application.chart_specs`.
- `render_product_explorer`, `render_retailer_average`, `render_price_movers`, and `render_overview` still exist and keep their current UI control labels.
- Aggregation formulas and mover formulas no longer appear inline in `streamlit_app.py`; the renderers call the use-case functions instead.
- `streamlit_app.py` still imports `plotly.express as px` and Streamlit, while files under `inflation_dashboard/application/` do not.
- `streamlit_app.py` does not import or call Falcon/HTTP clients in this phase.
</acceptance_criteria>
<verify>
Run source assertions that `streamlit_app.py` contains `calculate_retailer_average_trends`, `calculate_price_movers`, and `calculate_coverage_summary`, and that `inflation_dashboard/application/*.py` contains no `plotly`, `streamlit`, or `falcon` imports.
</verify>
</task>

<task id="01-02-T4" type="verification">
<name>Add and run focused ad-hoc verification for extracted core</name>
<read_first>
- `.planning/codebase/TESTING.md` existing ad-hoc verification guidance.
- `inflation_dashboard/domain/prices.py`.
- `inflation_dashboard/adapters/csv_price_repository.py`.
- `inflation_dashboard/application/use_cases.py`.
- `inflation_dashboard/application/chart_specs.py`.
- `streamlit_app.py`.
</read_first>
<action>
Create a temporary verification script under the OS temp directory, for example `Path(tempfile.gettempdir()) / "hermes-verify-inflation-dashboard-core.py"`, and run it from the repository root. The script must import extracted modules, scan `inflation_dashboard/` for forbidden framework imports (`streamlit`, `falcon`, `plotly` where `plotly` is forbidden only under `domain/`, `adapters/`, and `application/`), run in-memory use-case checks, discover real CSV inventory from `Datas/`, and when inventory is non-empty load one selected retailer/date window with `max_files_per_retailer=1`. The script must assert expected history columns, tuple-of-DataFrames loader shape, and non-empty representative use-case output when real rows are loaded. Do not commit the temp script.
</action>
<acceptance_criteria>
- The verification script path is outside the repository under the OS temp directory.
- The script exits with status 0 when run with `python <temp_script>` from the repo root.
- The script imports `inflation_dashboard.domain.prices`, `inflation_dashboard.adapters.csv_price_repository`, `inflation_dashboard.application.use_cases`, and `inflation_dashboard.application.chart_specs`.
- The script fails if extracted core imports Streamlit or Falcon, or if domain/application/adapters import Plotly.
- The script asserts loaded real history columns when real CSV inventory exists; if no inventory exists, it reports the skip explicitly rather than failing.
- The executor records the command and observed output in `01-02-SUMMARY.md`.
</acceptance_criteria>
<verify>
Run `python <temp_script>` and include the actual stdout/stderr and exit code in the plan summary.
</verify>
</task>
</tasks>

<verification>
- Execute all task-level verification commands in order.
- Run the temp OS verification script from task 01-02-T4 after all extraction and Streamlit wiring changes are complete.
- Re-run `python - <<'PY'` import checks for `streamlit_app`, `inflation_dashboard.domain.prices`, `inflation_dashboard.adapters.csv_price_repository`, `inflation_dashboard.application.use_cases`, and `inflation_dashboard.application.chart_specs` if the temp script fails and fixes are applied.
</verification>

<success_criteria>
- Application/use-case modules expose product history, retailer averages, price movers, and coverage calculations without Streamlit or Falcon imports.
- Chart-spec modules return dictionaries that Streamlit can convert into Plotly figures; no core module imports Plotly.
- Streamlit renderers use extracted core functions while preserving existing four dashboard tabs and user-facing controls.
- A bounded real CSV verification script under the OS temp directory exits 0 or explicitly reports no inventory if the repository has no usable CSV files.
- Phase 1 plans together cover REQ-02, REQ-05, REQ-06, REQ-07, REQ-08, REQ-10, and REQ-11.
</success_criteria>
